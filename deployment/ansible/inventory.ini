# Ansible Inventory for OSINT Platform

# Local deployment
[local]
localhost ansible_connection=local

# Remote deployment targets
[remote]
# Add your remote hosts here
# example-host ansible_host=192.168.1.100 ansible_user=deploy

# All OSINT platform hosts
[osint_platform:children]
local
remote

# Group variables
[osint_platform:vars]
osint_platform_path=/opt/osint-platform
docker_compose_file=docker-compose.yml